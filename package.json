{
  "name": "investment-platform-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build -w client",
    "install-client": "npm install -w client",
    "build-client": "npm run build -w client",
    "install-server": "npm install -w server",
    "start-server": "npm start -w server"
  },
  "workspaces": [
    "client",
    "server"
  ]
}